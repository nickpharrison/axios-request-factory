{
  "name": "axios-request-factory",
  "version": "0.1.1",
  "description": "A wrapper for Axios to handle various processes around making http(s) requests. Such as auth handling, queueing requests, rate-limiting etc",
  "main": "src/index.js",
  "types": "./types/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nick Harrison",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "axios": "^1.4.0"
  }
}
